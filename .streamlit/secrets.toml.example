# Copy this file to secrets.toml and fill in your actual values
# Do NOT commit secrets.toml to GitHub - it's in .gitignore

# Backend API URL (for local development)
api_base = "http://127.0.0.1:8000"

# For production deployment on Streamlit Cloud:
# api_base = "https://your-backend-api.com"

# If you deploy the backend separately, update the URL above
# The app will work in read-only mode without the backend
